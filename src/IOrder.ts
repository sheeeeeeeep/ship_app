export interface IOrder{
  my_name: string;
  my_pilot_id: string;
  my_created: string;
}

export interface Token{
  token: string;
}

export interface IStatus{
  my_name: string;
  my_pilot_id: string;
  my_status: string;
  night_shift: number;
  ship: string;
}
